#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
#define pb push_back
#define ll long long
#define ull unsigned long long
#define ld long double
void dbg_out() { cout << endl; }
template<typename Head, typename... Tail> void dbg_out(Head H, Tail... T) { cout << ' ' << H; dbg_out(T...); }
#define dbg(...) cout << "(" << #__VA_ARGS__ << "):", dbg_out(__VA_ARGS__)


int main()
  {


#ifndef ONLINE_JUDGE
  freopen("../../../in.in", "r", stdin);
  freopen("../../../out.out", "w", stdout);
#endif

  string s;
  cout << cin.eof() << endl;
  cin >> s;
  cout << cin.eof() << endl;
  cin >> s;
  cout << cin.eof() << endl;
  cin >> s;
  cout << cin.eof() << endl;

  // if(cin.eof() == true) to jest koniec pliku 

  // getline(cin >> ws, s); wczytujemy calą linijke
}